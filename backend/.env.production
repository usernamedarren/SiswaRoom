# Production Environment Configuration for aPanel Docker Deployment

# Database Configuration
DB_HOST=mysql
DB_USER=root
DB_PASSWORD=admin
DB_NAME=siswaroom
DB_PORT=3306

# JWT Configuration
JWT_SECRET=supersecretjwt_change_this_in_production
JWT_EXPIRE=7d

# Server Configuration
PORT=4000
NODE_ENV=production
API_URL=http://your-domain.com:4000

# CORS Configuration (adjust for your domain)
CORS_ORIGIN=http://your-domain.com,https://your-domain.com,http://localhost:5173

# Integration API Keys (change these in production!)
INTEGRATION_API_KEYS=prod-key-replace-me-123,partner-platform-key-replace-me

# MySQL Connection Pool
DB_POOL_SIZE=10
DB_WAIT_FOR_CONNECTIONS=true
DB_ENABLE_KEEP_ALIVE=true

# Logging
LOG_LEVEL=info
