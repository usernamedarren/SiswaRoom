node_modules
npm-debug.log
.git
.gitignore
*.local
.DS_Store
# Keep these for development in container:
# dist/ - will be built in container if needed
# src/ - needed for serving
# public/ - needed for static files
# index.html - needed for SPA
# vite.config.js - needed if running vite in container
