// Schedules UI removed - stub to avoid import errors
export async function initSchedules(container) {
  if (!container) return;
  container.innerHTML = `<p class="center text-gray">Fitur jadwal telah dihapus.</p>`;
}